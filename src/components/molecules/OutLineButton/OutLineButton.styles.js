import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        width: '100%',
        borderWidth: 1,
        color: '#FFFFFF',
        borderColor: '#223B67',
        alignItems: 'center',
        padding: 20
    },
    title: {
        color: '#223B67',
        fontWeight: 600,
        fontSize: 16
    }
});

export default styles;